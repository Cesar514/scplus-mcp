// Current goal: add structured function headers to the script-side static analysis helpers to eliminate the `function-header-3-lines` warnings in that file. \
// Rule: keep only incomplete tasks in this file and move completed work to `TODO_COMPLETED.md` immediately after verification. \
// Format: [] <priority>. <task> \
