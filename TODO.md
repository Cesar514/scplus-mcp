// Current goal: keep this TODO list accurate while implementing the user's current request. \
// Rule: complete all tasks asked by the user and verify they are fully completed before moving them to TODO_COMPLETED.md. \
// Format: [] <priority>. <task> \

[] 1. Audit remaining stale old-name references with blast-radius and repo sweeps, remove the active/public references touched by this goal, and leave no exact stale all-caps env-prefix matches anywhere in the repository. \
[] 2. Create one authoritative v1.5 control baseline before touching more engine code: synchronize `README.md`, `TODO.md`, `TODO_COMPLETED.md`, `plans/full-engine-roadmap.md`, `INSTRUCTIONS.md`, and any remote instruction endpoint so they all describe the same current storage contract, tool surface, roadmap reality, and product direction; remove stale references to dropped memory or ACP features and deleted legacy JSON layouts; synchronize version numbers across package metadata, MCP server metadata, release notes, and operator-visible banners; and add a maintenance rule that no milestone is complete until code, runtime behavior, and control documents match. \
[] 3. Encode the v1.5 non-negotiable rules directly into the repository workflow and review expectations: loud fatal errors over silent fallback, explicit `// FALLBACK` markers only where a framework-forced default cannot be removed, the active serving generation treated as the product contract, one shared backend truth source for MCP and the human CLI, and no claims of large-repo readiness or post-init speed until observability and benchmark gates pass. \
[] 4. Add explicit durable generation metadata to the sqlite-backed state, including generation id, mode, schema version, artifact versions, creation timestamp, validation timestamp, state (`staging`, `active`, `failed`, `superseded`), and any other fields needed to identify exactly what the engine is serving at query time. \
[] 5. Introduce an active-generation pointer and refactor every read path so exact lookup, related search, research, validation views, and operator diagnostics read only from one validated active generation instead of from mixed current state or partially refreshed artifacts. \
[] 6. Refactor `index`, `repair`, refresh, and any other rebuild path so work is always written into a candidate generation first, fully validated there, and only then atomically promoted to active status, with no possibility of partial artifacts leaking into live results during the build. \
[] 7. Guarantee last-known-good serving behavior by preserving the currently active validated generation whenever a new build, repair, refresh, migration, or provider-change path fails; the system must keep serving the old valid generation instead of dropping into empty, stale, or half-migrated state. \
[] 8. Make promotion to active status impossible unless every required artifact, vector collection, text artifact, stage-completion contract, and version contract is present and consistent; promotion failures must be loud, structured, and diagnosable rather than silently downgraded. \
[] 9. Expose generation identity and serving state through all public query surfaces and diagnostics so operators and agents can always see which generation answered the request, whether it was active or degraded, and why. \
[] 10. Add direct generation-safety tests that prove failed rebuilds, failed repairs, incompatible schema writes, and interrupted refreshes cannot corrupt the previously active generation and cannot expose partial state through search or exact-query tools. \
[] 11. Define a formal post-write freshness contract for `checkpoint`, `restore`, and every write-capable path so the system either refreshes affected artifacts deterministically before the next query or marks them explicitly dirty; remove any ambiguous “cache invalidated, probably okay” behavior. \
[] 12. Implement a per-file and per-generation dirty-state ledger so the backend can distinguish fresh exact-query state from stale semantic, chunk, vector, or structure state and avoid pretending all artifacts are equally current after local edits. \
[] 13. After `checkpoint`, trigger deterministic delta refresh for the exact-query substrate, file manifest state, identifier state, affected chunk state, affected vector entries, and any structure dependents of touched files, instead of relying on broad future refreshes or generic invalidation only. \
[] 14. After `restore`, trigger the same deterministic delta refresh path, explicitly invalidate any stale in-memory cache entries, and ensure the next query cannot observe restored files through old semantic or exact-query state. \
[] 15. Make freshness visible and enforceable in every query surface by surfacing freshness metadata when dependent artifacts are dirty and failing loudly when a request would otherwise consume invalid freshness state instead of silently serving stale answers. \
[] 16. Add focused delta-correctness tests for same-size content changes, restore-driven reversions, rename and move handling, delete handling, and dependency-driven downstream invalidation after local import or export changes. \
[] 17. Refactor the runtime into one shared long-lived backend service core that owns caches, prepared-state readers, embedding-provider state, generation-tracking logic, dirty ledgers, watcher ownership, and job scheduling, instead of splitting behavior between one MCP server path and repeated CLI subprocess invocations. \
[] 18. Replace the current Go backend shell-out pattern with a persistent local RPC or bridge channel so repeated `scplus-cli` actions do not pay repeated Node startup, module load, cache warmup, and bridge parsing cost. \
[] 19. Define one explicit structured bridge contract for commands, progress events, logs, degraded-state signals, machine-readable errors, and typed payloads so MCP and the human CLI are using the same backend semantics rather than similar but drifting command wrappers. \
[] 20. Make watcher ownership singular by moving file-watch state into the shared backend core so MCP, CLI, and future operator tools cannot race each other, double-watch the same repo, or enqueue duplicate rebuild work. \
[] 21. Add integration tests that prove MCP and the human CLI see the same active generation id, dirty-state markers, validation state, repair outcomes, and refresh outcomes when connected to the same repository through the shared backend. \
[] 22. Bring bridge parity up to the full engine by exposing the same essential surfaces already available through MCP, including `symbol`, `word`, `outline`, `deps`, `search`, `research`, `lint`, `blast_radius`, `checkpoint`, and `restore`, instead of leaving the CLI on a reduced subset. \
[] 23. Standardize one canonical argument and result contract per tool across MCP, bridge, and human CLI so parameter names, optional flags, structured fields, and error shapes cannot drift by transport. \
[] 24. Add parity tests that issue the same exact and related queries through MCP and bridge and assert equivalent structured outcomes, including generation metadata, degraded-state metadata, and result typing. \
[] 25. Audit every CLI-only or MCP-only behavior and either unify it behind the shared backend contract or remove the divergence if it does not materially improve operator or agent outcomes. \
[] 26. Install a minimal but real observability foundation before performance tuning: add structured metrics for stage duration, refresh duration, parser throughput, candidate counts, ranking latency, cache hits, cache misses, vector refresh writes, degraded-state frequency, and job queue state; add one diagnostic command that dumps generation state, dirty ledger, active jobs, cache readiness, vector collection metadata, and parser health in machine-readable form; and make these diagnostics consumable through MCP, bridge, and UI. \
[] 27. Audit the parser layer and remove silent degradation paths so grammar-load failures, parse failures, and unsupported-language paths become explicit degraded or fatal states rather than invisible regex substitution or “best effort” parsing. \
[] 28. Require every parser failure to record the file, parser backend, language, and reason for degradation and surface that through diagnostics and logs instead of burying it in transient console output. \
[] 29. Audit retrieval, validation, repair, and UI layers for hidden fallback behavior so missing vectors, missing lexical state, empty hybrid indexes, invalid prepared indexes, and misleading empty screens cannot quietly degrade into lower-fidelity answers or success-shaped UI. \
[] 30. Create and maintain a repository-wide inventory of every remaining allowed fallback path, why it still exists, what observable signal it emits, and what condition would permit its removal later; anything not in that inventory should be treated as a bug. \
[] 31. Add a lexical candidate generator, ideally FTS5 or an equivalent inverted-index layer, so related search can cheaply narrow file and identifier candidates before semantic reranking begins. \
[] 32. Refactor hybrid retrieval so semantic reranking works only on the candidate subset produced by lexical, structure, hub, or similar priors instead of scoring `Object.values(state.documents)` for the full corpus on every related search. \
[] 33. Refactor dense embedding search so it no longer loops over every vector entry in memory for each query when a smaller shortlist can be assembled first from lexical or structural candidates. \
[] 34. Tune candidate-set widths with actual latency and relevance measurements and record p50, p95, and p99 tradeoffs so default related-search settings are evidence-based instead of heuristic-only. \
[] 35. Add direct large-repo stress tests that prove exact lookup remains fast, related search remains bounded, and research latency remains controlled as repository size and artifact counts grow. \
[] 36. Replace full vector-namespace rewrites in the embedding save path with delta `upsertVectorEntries` plus explicit deletion of removed ids so refresh cost scales with changed content rather than total corpus size. \
[] 37. Stop loading complete vector namespaces into memory for serving paths that only need a bounded candidate set; move toward bounded or lazy vector access wherever the retrieval path allows it. \
[] 38. Add explicit vector collection metadata that records provider, model, dimensions, embedding contract version, and generation id so provider changes and model changes fail loudly instead of silently reusing incompatible caches. \
[] 39. Track vector refresh hit rate, miss rate, bytes written, entries rewritten, and refresh cost as first-class metrics so embedding maintenance can be optimized as an observable subsystem rather than guessed at. \
[] 40. Move embedding refresh out of ad hoc hot-path behavior and into an explicit service responsibility with visible scheduling, progress, and error reporting; only after that is stable and benchmarked should you consider moving stored vectors from JSON text blobs to a more compact binary representation. \
[] 41. Harden the fast exact-query substrate so deterministic navigation questions stay on the exact path instead of regressing into the heavier ranked engine whenever an exact answer is sufficient. \
[] 42. Add direct accuracy tests for symbol disambiguation, file outline correctness, dependency tracing, git status summaries, and git change summaries across rename, delete, conflict, and other high-risk repository states. \
[] 43. Audit exact-path result formatting so low-token answers remain compact but still carry enough structured metadata for automation, UI rendering, miss diagnosis, and generation-aware debugging. \
[] 44. Expose explicit reason codes for exact-path misses so the engine can distinguish true not-found, dirty-generation state, unsupported-language state, stale-index state, and other failure classes without forcing agents to guess. \
[] 45. Add explanation metadata to ranked results so operators and agents can inspect which evidence sources contributed to each score, such as exact lexical hits, identifier overlap, chunk similarity, structure priors, or hub priors. \
[] 46. Remove brittle heuristics from unified ranking, especially any metadata shortcuts that classify entities by label strings or loose text markers instead of explicit typed contracts. \
[] 47. Add calibration tests that prove `search` with exact intent reliably avoids expensive related-search behavior and that related intent still returns the expected files and symbols after the candidate-first rewrite. \
[] 48. Strengthen `research` verification so broad subsystem reports are checked for source grounding, stable structure, and duplication control across rebuilds rather than only for superficial output presence. \
[] 49. Add a ranking-debug mode that exposes candidate counts, evidence contributions, rerank decisions, and final ordering without requiring ad hoc source edits. \
[] 50. Optimize parser and structure-path performance by pooling or reusing Tree-sitter parser instances where safe, removing redundant grammar-file reads before `Parser.Language.load()` if they are unnecessary, and measuring parser throughput by language and file size as part of observability. \
[] 51. Audit regex fallback helpers and brace-based range heuristics so they are either removed entirely or explicitly marked and tested as degraded approximations with known limits. \
[] 52. Refactor watcher behavior into a real job queue with explicit states such as queued, running, blocked, superseded, succeeded, and failed so file bursts collapse into one well-scoped refresh or rebuild flow instead of repeated broad work. \
[] 53. Teach the queue to cancel or supersede stale refresh jobs when a newer file-change batch makes earlier work irrelevant, while preserving audit logs for why each job was dropped or merged. \
[] 54. Distinguish delta refresh jobs from full rebuild jobs in both scheduler logic and operator reporting so the product does not feel noisy, random, or more expensive than necessary. \
[] 55. Expose current queue state, active job, generation target, pending dirty files, and last failure through diagnostics and the human CLI, then add tests for watcher storms, repeated file rewrites, directory renames, and overlapping manual index plus auto-refresh workflows. \
[] 56. Redesign `scplus-cli` around structured panes and typed data rather than mostly raw text dumps so operators can browse lists, inspect details, and take actions without losing context or trusting unstructured blobs. \
[] 57. Once the new interaction model stabilizes, split oversized UI state and rendering modules such as `cli/internal/ui/model.go` into smaller state, update, action, and rendering files so the UI no longer depends on an elevated lint size cap. \
[] 58. Make the overview screen scrollable and status-driven, replace monolithic text views with list, table, or tree widgets for hubs, restore points, search results, jobs, and diagnostics, and add a preview pane for the selected item. \
[] 59. Add a command palette plus keyboard-driven filter, search-inside-results, and navigation history so the operator console becomes a real control surface instead of a dashboard-only wrapper. \
[] 60. Expose validate, repair, exact search, related search, research, lint, blast radius, checkpoint, restore, and hub workflows directly in the console once bridge parity is complete. \
[] 61. Add explicit degraded-state, generation-state, and freshness badges everywhere in the UI so the operator always knows whether the visible data is current, stale, degraded, or blocked. \
[] 62. Fix watcher lifecycle and shutdown issues in the UI, including double-close or repeated-close paths, and ensure CLI shutdown is idempotent and panic-free under repeated quit signals. \
[] 63. Keep the synthetic evaluation suite, but add a real-repo benchmark tier covering small, medium, large, and polyglot repositories plus at least one intentionally broken or messy repository shape so claims are not based on fixture repos alone. \
[] 64. Split oversized benchmark modules such as `src/tools/evaluation.ts` into smaller scenario and reporting modules once the benchmark surface stabilizes so the harness no longer depends on elevated lint size caps. \
[] 65. Create a golden question set for exact navigation, related discovery, dependency tracing, and subsystem research so regressions are measured against stable expected outcomes rather than ad hoc spot checks. \
[] 66. Benchmark initialization cost separately from steady-state query cost so the product can intentionally accept heavier upfront preparation while still enforcing low-latency serving after initialization. \
[] 67. Add regression gates for same-size edits, rename-heavy diffs, delete-heavy diffs, restore-driven rewinds, and large-repo memory and latency behavior so refresh correctness and serving performance are proven under realistic churn. \
[] 68. Refuse to label the engine optimized after initialization until the generation-safety suite, bridge parity suite, freshness suite, observability checks, and real-repo benchmark pack all pass in a reproducible local or CI-driven workflow. \
[] 69. Define the v1.5 release gate explicitly: passing build, full test suite, generation-safety suite, post-write freshness suite, bridge parity suite, observability checks, and real-repo benchmark pack, with synchronized docs and version metadata as required exit criteria. \
[] 70. Prevent net-new public tool additions during v1.5 unless they clearly improve codebase context quality and do not delay serving, parity, correctness, and performance work; after v1.5, audit feature and parameter sprawl and remove or merge overlapping surfaces that do not materially improve agent or operator outcomes. \
[] 71. Only after all of the above is complete, benchmarked, and truth-synchronized, consider optional advanced follow-up work such as ANN backends, broader `researchplus` features, richer automation layers, or other new product surfaces. \
