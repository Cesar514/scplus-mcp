// summary: Defines OpenNext deployment configuration for the scplus landing target.
// FEATURE: Landing deployment settings for the Cloudflare OpenNext target.
// inputs: Cloudflare OpenNext deployment settings and build output options.
// outputs: Exported OpenNext configuration for landing deployment.
import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({});
